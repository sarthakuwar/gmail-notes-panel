node_modules/
.env
.env.*
supabase/.env.local
*.pem

# Local agent tooling, not part of the shipped product
.agents/
.claude/
skills-lock.json

# Supabase CLI local state (created by `supabase link`/`secrets set`)
supabase/.temp/

# Chrome Web Store upload package (rebuild with the "package the extension" step)
dist/
